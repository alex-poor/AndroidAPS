<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application>

        <service android:name=".general.persistentNotification.DummyService" />

        <!-- Not exported: it manages files in app-private storage and is only ever reached from Settings. -->
        <activity
            android:name=".general.themes.SkinManagerActivity"
            android:exported="false"
            android:label="Skins" />

    </application>

</manifest>
